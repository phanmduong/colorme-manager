import React from 'react';
import Icon from './Icon';

const TabIcon = ({nameIcon, tintColor}) => (
            <Icon
                name={nameIcon}
                size={26}
                color="white"
            />
);

export default TabIcon;
