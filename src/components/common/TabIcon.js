import React from 'react';
import Icon from './Icon';

const TabIcon = ({nameIcon, color}) => (
    <Icon
        name={nameIcon}
        size={26}
        color={color}
    />
);

export default TabIcon;
